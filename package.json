{
  "name": "glados-workers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "cf:types": "wrangler types",
    "db:prepare": "node scripts/prepare-d1.mjs",
    "db:migrate:remote": "node scripts/migrate-d1.mjs",
    "dev": "wrangler dev --local --host 127.0.0.1 --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.10",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.18.0"
  }
}
