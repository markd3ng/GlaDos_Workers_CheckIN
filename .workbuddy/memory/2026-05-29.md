# 2026-05-29 工作日志

## README 重写

- 完整重写了 GLaDOS Workers Check-In 项目的 README.md
- 从原来的特性列表文档转变为手把手部署指南（545 行 vs 原 521 行，结构完全重排）
- 核心改进：
  - Cloudflare API Token 创建：从 "建议使用权限最小化" 变成完整点击路径（avatar → My Profile → API Tokens → Create Custom Token），明确列出三行权限（Workers Scripts:Edit, D1:Edit, Account Settings:Read）
  - GitHub Secrets vs Cloudflare Variables 彻底分离，不再混在一段里
  - Cookie 获取步骤详细到 Network 面板筛选、请求头定位
  - 每步都有验证方法（应返回 ... / 应看到 ...）
  - 排障部分覆盖了源码中所有 `throw new Error(...)` 场景
  - 所有 12 个代码块均带语言标记
- 方法论输出为 user-level skill `readme-from-scratch`（路径：~/.workbuddy/skills/readme-from-scratch/），可供 Codex 复用
- 提交并 push 到 main 分支：commit `38ea481`
